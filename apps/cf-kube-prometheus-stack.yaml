apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cf-kube-prometheus-stack
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/name: cf-kube-prometheus-stack
spec:
  project: default

  source:
    repoURL: https://github.com/lwooden/cf-core-cluster-services.git
    targetRevision: main
    path: cf-kube-prometheus-stack
    helm:
      releaseName: kube-prometheus-stack

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
