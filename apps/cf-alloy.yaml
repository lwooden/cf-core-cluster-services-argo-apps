apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cf-alloy
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/name: cf-alloy
spec:
  project: default

  source:
    repoURL: https://github.com/lwooden/cf-core-cluster-services.git
    targetRevision: main
    path: cf-alloy
  #   helm:
  #     releaseName: cf-ingress-haproxy

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
