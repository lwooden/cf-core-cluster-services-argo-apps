apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cf-external-secrets
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/name: cf-external-secrets
spec:
  project: default

  source:
    repoURL: https://github.com/lwooden/cf-core-cluster-services.git
    targetRevision: main
    path: cf-external-secrets
  #   helm:
  #     releaseName: cf-ingress-haproxy

  destination:
    server: https://kubernetes.default.svc
    namespace: external-secrets

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
